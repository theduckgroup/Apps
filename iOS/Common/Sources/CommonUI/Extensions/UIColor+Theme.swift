import Foundation
import UIKit

public extension UIColor {
    /// Lighter theme color (used for dark UI style).
    static let themeLight = UIColor(hex: 0x2E7DB1)
    // static let themeLight = UIColor(hex: 0x2F80AD) // Too light
    
    /// Darker theme color (used for light UI style).
    static let themeDark = UIColor(hex: 0x2C6494)
    // static let themeDark = UIColor(hex: 0x286090) // Too dark
}
