import Foundation
import SwiftUI

public extension Color {
    static let theme = Color(hex: 0x2E7DB1)
    
    /// Very dark color used in NakedBlendCalc for nav bar, not useful for text etc.
    static let themeAlt = Color(hex: 0x153850)
}
